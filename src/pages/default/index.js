import React from 'react';

import './style';

class Default extends React.Component {
  render() {
    return (
        <button>bbb</button>
    );
  }
}

let route = {
  component: Default
};

export default route;